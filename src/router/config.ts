const routes = [
  {
    path: ["/", "/home"],
    exact: true,
    component: "Home",
  },
  {
    path: ["/Schedule"],
    exact: true,
    component: "Schedule",
  },
];

export default routes;
